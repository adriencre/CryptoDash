# Profil dev : base de données MySQL (Docker)
# Démarrer MySQL : docker compose up -d
# Puis lancer le backend : mvn spring-boot:run
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/cryptodash?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Europe/Paris
    username: cryptodash
    password: cryptodash
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
